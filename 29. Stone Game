class Solution {
    public boolean stoneGame(int[] piles) {

        return true;
        // int sum1 = 0;
        // int sum2 = 0;
        // int i = 0;
        // int j = piles.length - 1;
        // while (i <= j) {

        //     if (piles[i] > piles[j]) {
        //         sum1 += piles[i];
        //         i++;
        //     } else {
        //         sum1 += piles[j];
        //         j--;
        //     }

        //     if (i <= j) { // Check to prevent index out of bounds
        //         if (piles[i] > piles[j]) {
        //             sum2 += piles[i];
        //             i++;
        //         } else {
        //             sum2 += piles[j];
        //             j--;
        //         }
        //     }
        // }

        // return sum1 > sum2;
    }
}
