class Solution {
    public int smallestNumber(int n) {
        
        int x = 0;
        int power = 0;
        while(x<n){

            x+= Math.pow(2,power);
            power++;
            
        }
        return x;
    }
}
